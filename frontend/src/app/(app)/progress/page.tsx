'use client'

import { useState, useEffect, useMemo } from 'react'
import Link from 'next/link'

import { useTranslations } from 'next-intl'
import { Award, BookOpen, Check, Circle, Flame, LoaderCircle, Sparkles, type LucideIcon } from 'lucide-react'
import { PageLoading } from '@/components/ui/page-loading'
import { apiFetch } from '@/lib/api'
import { useLanguageStore } from '@/store/language'
import NoPlanBanner from '@/components/plan/NoPlanBanner'
import { getCurriculumUnits, type CurriculumUnit, type CEFRLevel } from '@/data/curriculum'
import type { VocabularySet } from '@/data/types'

interface CompetencyRecord { unit_id: string; score: number; mastered_count: number; total_count: number }
interface ProgressSummary { total_xp: number; current_streak: number; total_lessons: number; total_exercises: number; exercises_correct: number; accuracy: number; skills: Record<string, number>; vocabulary_level?: string; vocabulary_mastered?: number; vocabulary_total?: number; vocabulary_progress?: number }
interface FlashcardProgress { id: number; word: string; repetitions: number }
interface StudyPlan { id: number; cefr_level: string }
interface HistoryEntry { id: number; user_id: number; date: string; xp_earned: number; lessons_completed: number; exercises_correct: number; exercises_total: number; streak_day: number; skills: Record<string, number> }
interface GameStats { total_xp: number; games_played: number; questions_answered: number; correct_answers: number; best_round_score: number; daily_challenges_completed: number; last_daily_challenge_date: string; current_correct_streak: number; best_correct_streak: number; achievements: string[]; skills: Record<string, number> }
interface GoalMilestoneSummary { total_milestones: number; daily_milestones: number; weekly_milestones: number; total_reward_xp: number; daily_reward_xp: number; weekly_reward_xp: number }
interface GoalMilestone { id: number; goal_type: 'daily' | 'weekly'; period_start: string; period_end: string; target_xp: number; achieved_xp: number; reward_xp: number; achieved_at: string }
interface LearningGoals { daily_xp_target: number; weekly_xp_target: number; daily_xp: number; weekly_xp: number; daily_progress: number; weekly_progress: number; daily_completed: boolean; weekly_completed: boolean; daily_reward_xp: number; weekly_reward_xp: number; daily_reward_claimed: boolean; weekly_reward_claimed: boolean; day: string; week_start: string; week_end: string }
interface HistoryRangeSummary { period: 'week' | 'month' | 'all'; from_date?: string | null; to_date?: string | null; total_xp: number; total_lessons: number; total_exercises: number; exercises_correct: number; accuracy: number; active_days: number; average_daily_xp: number; skills: Record<string, number> }
type CompetencyStatus = 'mastered' | 'in-progress' | 'not-started'

function getCompetencyStatus(itemIndex: number, masteredCount: number, totalCount: number, score: number): CompetencyStatus { if (itemIndex < masteredCount) return 'mastered'; if (score > 0 && itemIndex < totalCount) return 'in-progress'; return 'not-started' }
const STATUS_BADGE: Record<CompetencyStatus, { Icon: LucideIcon; color: string; background: string }> = { mastered: { Icon: Check, color: '#ffffff', background: 'var(--juba-primary)' }, 'in-progress': { Icon: LoaderCircle, color: 'var(--juba-warm)', background: 'var(--juba-warm-soft)' }, 'not-started': { Icon: Circle, color: 'var(--juba-muted)', background: 'transparent' } }

function UnitCompetencyBlock({ unit, record }: { unit: CurriculumUnit; record: CompetencyRecord | undefined }) {
  const t = useTranslations('progress'); const tPlan = useTranslations('plan'); const masteredCount = record?.mastered_count ?? 0; const totalCount = unit.competency_checklist.length; const score = record?.score ?? 0; const pct = totalCount > 0 ? Math.round((masteredCount / totalCount) * 100) : 0; const complete = pct === 100
  return <div className={`border-fl-border bg-fl-surface rounded-2xl border ${complete ? '' : 'overflow-hidden'}`}><div className="flex items-center justify-between gap-4 px-5 pt-4 pb-3"><div className="min-w-0"><span className="text-xs font-semibold" style={{ color: 'var(--juba-primary)' }}>{tPlan('unitLabel')} {unit.unit_number}</span><p className="text-fl-fg truncate text-sm font-bold">{unit.title}</p></div><div className="flex shrink-0 items-center gap-3"><span className="text-fl-muted-3 text-xs font-medium">{masteredCount}/{totalCount} {t('mastered')}</span>{record && <span className="text-fl-muted-1 text-xs font-semibold tabular-nums">{Math.round(score * 100)}%</span>}</div></div><div className="bg-fl-surface-2 mx-5 mb-3 h-1.5 overflow-hidden rounded-full"><div role="progressbar" aria-valuemin={0} aria-valuemax={100} aria-valuenow={pct} className="h-full rounded-full transition-all duration-500" style={{ width: `${pct}%`, background: complete ? 'var(--juba-primary)' : 'var(--juba-warm)' }} /></div><ul className="space-y-2.5 px-5 pb-4">{unit.competency_checklist.map((text, idx) => { const status = getCompetencyStatus(idx, masteredCount, record?.total_count ?? 0, score); const { Icon, color, background } = STATUS_BADGE[status]; return <li key={idx} className="flex items-start gap-3"><span className="mt-0.5 flex h-4.5 w-4.5 shrink-0 items-center justify-center rounded-full" style={status === 'not-started' ? { border: '1px solid var(--juba-border)' } : { background }}><Icon className="h-3 w-3" style={{ color }} aria-hidden="true" /></span><span className={`flex-1 text-xs leading-relaxed ${status === 'mastered' ? 'text-fl-muted-2' : status === 'in-progress' ? 'text-fl-fg font-medium' : 'text-fl-muted-4'}`}>{text}</span>{status === 'in-progress' && record && <span className="text-fl-muted-3 shrink-0 text-xs tabular-nums">{Math.round(score * 100)}%</span>}</li> })}</ul></div>
}

export default function ProgressPage() {
  const t = useTranslations('progress'); const tVocab = useTranslations('vocabulary'); const activeLanguage = useLanguageStore((s) => s.activeLanguage)
  const [goals, setGoals] = useState<LearningGoals | null>(null); const [goalMilestoneSummary, setGoalMilestoneSummary] = useState<GoalMilestoneSummary | null>(null); const [goalHistory, setGoalHistory] = useState<GoalMilestone[]>([]); const [dailyGoalTarget, setDailyGoalTarget] = useState('50'); const [weeklyGoalTarget, setWeeklyGoalTarget] = useState('250'); const [savingGoals, setSavingGoals] = useState(false); const [summary, setSummary] = useState<ProgressSummary | null>(null); const [gameStats, setGameStats] = useState<GameStats | null>(null); const [historyRange, setHistoryRange] = useState<'week' | 'month' | 'all'>('week'); const [rangeSummary, setRangeSummary] = useState<HistoryRangeSummary | null>(null); const [summaryRangeLoading, setSummaryRangeLoading] = useState(false); const [competencies, setCompetencies] = useState<CompetencyRecord[]>([]); const [plan, setPlan] = useState<StudyPlan | null>(null); const [loading, setLoading] = useState(true); const [levelUnits, setLevelUnits] = useState<CurriculumUnit[]>([]); const [flashcards, setFlashcards] = useState<FlashcardProgress[]>([]); const [showAllLevels, setShowAllLevels] = useState(false); const [history, setHistory] = useState<HistoryEntry[]>([])

  useEffect(() => { async function load() { try { const [sumRes, compRes, planRes, flashRes, gameRes, goalsRes] = await Promise.all([apiFetch('/api/progress/summary'), apiFetch('/api/progress/competencies'), apiFetch('/api/study-plan/current'), apiFetch('/api/flashcards/all').catch(() => null), apiFetch('/api/progress/game-summary').catch(() => null), apiFetch('/api/progress/goals').catch(() => null)]); if (sumRes.ok) setSummary((await sumRes.json()) as ProgressSummary); if (gameRes?.ok) setGameStats((await gameRes.json()) as GameStats); if (goalsRes?.ok) { const nextGoals = (await goalsRes.json()) as LearningGoals; setGoals(nextGoals); setDailyGoalTarget(String(nextGoals.daily_xp_target)); setWeeklyGoalTarget(String(nextGoals.weekly_xp_target)); } if (compRes.ok) setCompetencies((await compRes.json()) as CompetencyRecord[]); if (planRes.ok) setPlan((await planRes.json()) as StudyPlan); if (flashRes?.ok) setFlashcards((await flashRes.json()) as FlashcardProgress[]) } catch { /* ignore */ } finally { setLoading(false) } } void load() }, [activeLanguage?.code])

  useEffect(() => {
    let cancelled = false
    apiFetch('/api/progress/goals/milestones/summary')
      .then((res) => res.ok ? res.json() as Promise<GoalMilestoneSummary> : Promise.reject(new Error('goal milestone summary failed')))
      .then((data) => { if (!cancelled) setGoalMilestoneSummary(data) })
      .catch(() => { if (!cancelled) setGoalMilestoneSummary(null) })
    return () => { cancelled = true }
  }, [activeLanguage?.code])
  useEffect(() => {
    let cancelled = false
    apiFetch('/api/progress/goals/history?limit=12')
      .then((res) => res.ok ? res.json() as Promise<GoalMilestone[]> : Promise.reject(new Error('goal history failed')))
      .then((data) => { if (!cancelled) setGoalHistory(data) })
      .catch(() => { if (!cancelled) setGoalHistory([]) })
    return () => { cancelled = true }
  }, [activeLanguage?.code])
  useEffect(() => {
    let cancelled = false
    apiFetch(`/api/progress/history?range=${historyRange}`)
      .then((res) => res.ok ? res.json() as Promise<{ entries?: HistoryEntry[] }> : Promise.reject(new Error('history request failed')))
      .then((data) => { if (!cancelled) setHistory(data.entries ?? []) })
      .catch(() => { if (!cancelled) setHistory([]) })
    return () => { cancelled = true }
  }, [historyRange, activeLanguage?.code])
  useEffect(() => {
    let cancelled = false
    setSummaryRangeLoading(true)
    apiFetch(`/api/progress/history/summary?range=${historyRange}`)
      .then(async (response) => {
        if (!response.ok) throw new Error('history summary failed')
        return (await response.json()) as HistoryRangeSummary
      })
      .then((data) => { if (!cancelled) setRangeSummary(data) })
      .catch(() => { if (!cancelled) setRangeSummary(null) })
      .finally(() => { if (!cancelled) setSummaryRangeLoading(false) })
    return () => { cancelled = true }
  }, [historyRange, activeLanguage?.code])

  const targetLanguageCode = activeLanguage?.code ?? 'en-GB'
  useEffect(() => { if (plan?.cefr_level) getCurriculumUnits(plan.cefr_level, targetLanguageCode).then(setLevelUnits).catch(() => setLevelUnits([])) }, [plan?.cefr_level, targetLanguageCode])
  const [vocabSets, setVocabSets] = useState<VocabularySet[]>([])
  useEffect(() => { apiFetch(`/api/vocabulary?language=${targetLanguageCode}`).then((r) => r.json()).then((d: { sets: VocabularySet[] }) => setVocabSets(d.sets)).catch(() => setVocabSets([])) }, [targetLanguageCode])
  const compMap = Object.fromEntries(competencies.map((c) => [c.unit_id, c]))
  const recentHistory = useMemo(() => history.slice(0, historyRange === 'week' ? 7 : historyRange === 'month' ? 30 : history.length).reverse(), [history, historyRange])
  const maxDailyXp = Math.max(1, ...recentHistory.map((entry) => entry.xp_earned))
  if (loading) return <PageLoading label={t('loading')} />
  if (!plan) return <NoPlanBanner />
  const cefrLevel = plan.cefr_level as CEFRLevel; const displayVocabSets = showAllLevels ? vocabSets : vocabSets.filter((s) => s.level === cefrLevel); const totalDisplayWords = displayVocabSets.reduce((a, s) => a + s.words.length, 0)
  const masteredWordSet = new Set(flashcards.filter((f) => f.repetitions > 0).map((f) => f.word.toLowerCase())); const totalMastered = displayVocabSets.reduce((a, s) => a + s.words.filter((w) => masteredWordSet.has(w.word.toLowerCase())).length, 0)
  const statTiles = summary ? [{ label: t('xp'), value: summary.total_xp.toLocaleString(), Icon: Sparkles, highlight: false }, { label: t('streak'), value: `${summary.current_streak} ${t('days')}`, Icon: Flame, highlight: summary.current_streak > 0 }, { label: t('lessons'), value: summary.total_lessons, Icon: BookOpen, highlight: false }, { label: t('accuracy'), value: `${Math.round(summary.accuracy * 100)}%`, Icon: Check, highlight: false }] : []

  return <div className="mx-auto max-w-4xl space-y-8 px-4 py-6 sm:px-6 md:py-8">
    <div className="border-fl-border bg-fl-surface rounded-2xl border p-5 sm:p-6"><div className="mb-4 flex flex-wrap items-center justify-between gap-3"><h1 className="text-fl-fg text-lg font-bold tracking-tight">{t('subtitle')}</h1>{activeLanguage && cefrLevel && <span className="rounded-full px-3 py-1 text-xs font-semibold" style={{ color: 'var(--juba-primary-dark)', background: 'var(--juba-primary-soft)' }}>{activeLanguage.name} · {cefrLevel}</span>}</div>{statTiles.length > 0 && <div className="grid grid-cols-2 gap-3 sm:grid-cols-4">{statTiles.map((stat) => <div key={stat.label} className="bg-fl-surface-2 rounded-xl p-3.5 text-center sm:text-start"><div className="mb-1.5 flex items-center justify-center gap-1.5 sm:justify-start"><stat.Icon className={`h-3.5 w-3.5 ${stat.highlight ? 'text-[var(--juba-warm)]' : 'text-[var(--juba-primary)]'}`} aria-hidden="true" /><p className="text-fl-muted-2 truncate text-xs font-medium">{stat.label}</p></div><p className={`text-xl font-bold tracking-tight ${stat.highlight ? 'text-[var(--juba-warm)]' : 'text-fl-fg'}`}>{stat.value}</p></div>)}</div>}</div>

    {goals && <section className="border-fl-border bg-fl-surface rounded-2xl border p-5 sm:p-6"><div className="mb-5 flex items-center justify-between gap-3"><div><span className="text-fl-muted-3 text-xs font-mono tracking-widest uppercase">{t('goals')}</span><h2 className="text-fl-fg mt-1 text-lg font-bold">{t('dailyWeeklyGoals')}</h2></div><button type="button" disabled={savingGoals} onClick={async () => { const daily = Number(dailyGoalTarget); const weekly = Number(weeklyGoalTarget); if (!Number.isInteger(daily) || !Number.isInteger(weekly) || daily < 1 || weekly < 1) return; setSavingGoals(true); try { const response = await apiFetch('/api/progress/goals', { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ daily_xp_target: daily, weekly_xp_target: weekly }) }); if (response.ok) { const nextGoals = (await response.json()) as LearningGoals; setGoals(nextGoals); setDailyGoalTarget(String(nextGoals.daily_xp_target)); setWeeklyGoalTarget(String(nextGoals.weekly_xp_target)); } } finally { setSavingGoals(false) } }} className="rounded-lg px-3 py-1.5 text-xs font-semibold text-white disabled:opacity-50" style={{ background: 'var(--juba-primary)' }}>{savingGoals ? t('loading') : t('saveGoals')}</button></div><div className="grid gap-4 sm:grid-cols-2"><div className="bg-fl-surface-2 rounded-xl p-4"><div className="mb-2 flex items-center justify-between gap-3"><div><p className="text-fl-fg text-sm font-bold">{t('dailyGoal')}</p><p className="text-fl-muted-3 text-xs">{goals.daily_xp} / {goals.daily_xp_target} {t('xp')}</p>{goals.daily_reward_claimed && <span className="mt-1 inline-flex items-center gap-1 rounded-full px-2 py-0.5 text-[10px] font-bold" style={{ color: 'var(--juba-primary-dark)', background: 'var(--juba-primary-soft)' }}><Check className="h-3 w-3" aria-hidden="true" />+{goals.daily_reward_xp} {t('rewardXp')}</span>}</div><span className="text-fl-fg text-sm font-bold">{Math.round(goals.daily_progress * 100)}%</span></div><div className="bg-fl-border h-2 overflow-hidden rounded-full"><div className="h-full rounded-full transition-all" style={{ width: (Math.round(goals.daily_progress * 100) + '%'), background: 'var(--juba-primary)' }} /></div><label className="text-fl-muted-3 mt-4 block text-xs">{t('dailyTarget')}<input type="number" min="1" max="10000" value={dailyGoalTarget} onChange={(event) => setDailyGoalTarget(event.target.value)} className="border-fl-border bg-fl-surface text-fl-fg mt-1 w-full rounded-lg border px-3 py-2 text-sm" /></label></div><div className="bg-fl-surface-2 rounded-xl p-4"><div className="mb-2 flex items-center justify-between gap-3"><div><p className="text-fl-fg text-sm font-bold">{t('weeklyGoal')}</p><p className="text-fl-muted-3 text-xs">{goals.weekly_xp} / {goals.weekly_xp_target} {t('xp')}</p>{goals.weekly_reward_claimed && <span className="mt-1 inline-flex items-center gap-1 rounded-full px-2 py-0.5 text-[10px] font-bold" style={{ color: 'var(--juba-primary-dark)', background: 'var(--juba-primary-soft)' }}><Check className="h-3 w-3" aria-hidden="true" />+{goals.weekly_reward_xp} {t('rewardXp')}</span>}</div><span className="text-fl-fg text-sm font-bold">{Math.round(goals.weekly_progress * 100)}%</span></div><div className="bg-fl-border h-2 overflow-hidden rounded-full"><div className="h-full rounded-full transition-all" style={{ width: (Math.round(goals.weekly_progress * 100) + '%'), background: 'var(--juba-primary)' }} /></div><label className="text-fl-muted-3 mt-4 block text-xs">{t('weeklyTarget')}<input type="number" min="1" max="70000" value={weeklyGoalTarget} onChange={(event) => setWeeklyGoalTarget(event.target.value)} className="border-fl-border bg-fl-surface text-fl-fg mt-1 w-full rounded-lg border px-3 py-2 text-sm" /></label></div></div></section>}

    {goalHistory.length > 0 && <section className="border-fl-border bg-fl-surface rounded-2xl border p-5 sm:p-6"><div className="mb-4 flex items-center justify-between gap-3"><div><span className="text-fl-muted-3 text-xs font-mono tracking-widest uppercase">{t('goalHistory')}</span><h2 className="text-fl-fg mt-1 text-lg font-bold">{t('milestones')}</h2></div><span className="text-fl-muted-2 text-xs">{goalMilestoneSummary?.total_reward_xp ?? goalHistory.reduce((sum, item) => sum + item.reward_xp, 0)} {t('rewardXp')}</span></div><div className="divide-fl-border divide-y">{goalHistory.map((item) => <div key={item.id} className="flex items-center justify-between gap-4 py-3 first:pt-0 last:pb-0"><div className="min-w-0"><p className="text-fl-fg text-sm font-semibold">{item.goal_type === 'daily' ? t('dailyGoal') : t('weeklyGoal')}</p><p className="text-fl-muted-3 text-xs">{item.period_start} · {item.achieved_xp}/{item.target_xp} {t('xp')}</p></div><span className="shrink-0 rounded-full px-2.5 py-1 text-xs font-bold" style={{ color: 'var(--juba-primary-dark)', background: 'var(--juba-primary-soft)' }}>+{item.reward_xp} {t('xp')}</span></div>)}</div></section>}

    {recentHistory.length > 0 && <section className="border-fl-border bg-fl-surface rounded-2xl border p-5 sm:p-6"><div className="mb-5 flex flex-wrap items-center justify-between gap-3"><div><span className="text-fl-muted-3 text-xs font-mono tracking-widest uppercase">{t('learningActivity')}</span><h2 className="text-fl-fg mt-1 text-lg font-bold">{historyRange === 'week' ? t('last7Days') : historyRange === 'month' ? t('last30Days') : t('allTime')}</h2></div><div className="bg-fl-surface-2 inline-flex rounded-xl p-1" role="group" aria-label={t('activityRange')}><button type="button" aria-pressed={historyRange === 'week'} onClick={() => setHistoryRange('week')} className={`rounded-lg px-3 py-1.5 text-xs font-semibold ${historyRange === 'week' ? 'text-fl-fg bg-[var(--juba-surface)] shadow-sm' : 'text-fl-muted-2'}`}>{t('sevenDays')}</button><button type="button" aria-pressed={historyRange === 'month'} onClick={() => setHistoryRange('month')} className={`rounded-lg px-3 py-1.5 text-xs font-semibold ${historyRange === 'month' ? 'text-fl-fg bg-[var(--juba-surface)] shadow-sm' : 'text-fl-muted-2'}`}>{t('thirtyDays')}</button><button type="button" aria-pressed={historyRange === 'all'} onClick={() => setHistoryRange('all')} className={`rounded-lg px-3 py-1.5 text-xs font-semibold ${historyRange === 'all' ? 'text-fl-fg bg-[var(--juba-surface)] shadow-sm' : 'text-fl-muted-2'}`}>{t('allTime')}</button></div><Link href="/vocabulary/review" className="text-sm font-bold" aria-label={t('reviewLink')} style={{ color: 'var(--juba-primary)' }}>{t('reviewLink')} →</Link></div><div className="grid grid-cols-7 items-end gap-2 sm:gap-3">{recentHistory.map((entry) => { const height = Math.max(8, Math.round((entry.xp_earned / maxDailyXp) * 100)); const dateLabel = new Date(`${entry.date}T00:00:00`).toLocaleDateString(undefined, { weekday: 'short' }); return <div key={entry.date} className="flex min-w-0 flex-col items-center gap-2"><span className="text-fl-muted-3 text-[10px] tabular-nums">{entry.xp_earned} {t('xp')}</span><div className="bg-fl-surface-2 flex h-28 w-full max-w-10 items-end overflow-hidden rounded-lg"><div className="w-full rounded-t-lg transition-all" style={{ height: `${height}%`, background: 'var(--juba-primary)' }} /></div><span className="text-fl-muted-3 truncate text-[10px] font-medium">{dateLabel}</span></div> })}</div>{rangeSummary && <div className="mt-5 grid grid-cols-2 gap-3 border-t border-fl-border pt-4 text-center sm:grid-cols-4"><div><p className="text-fl-fg text-lg font-black">{rangeSummary.total_xp}</p><p className="text-fl-muted-3 text-[10px] font-mono tracking-widest uppercase">{t('rangeXp')}</p></div><div><p className="text-fl-fg text-lg font-black">{Math.round(rangeSummary.accuracy * 100)}%</p><p className="text-fl-muted-3 text-[10px] font-mono tracking-widest uppercase">{t('rangeAccuracy')}</p></div><div><p className="text-fl-fg text-lg font-black">{rangeSummary.active_days}</p><p className="text-fl-muted-3 text-[10px] font-mono tracking-widest uppercase">{t('activeDays')}</p></div><div><p className="text-fl-fg text-lg font-black">{rangeSummary.average_daily_xp}</p><p className="text-fl-muted-3 text-[10px] font-mono tracking-widest uppercase">{t('avgDailyXp')}</p></div></div>}{summaryRangeLoading && <p className="text-fl-muted-3 mt-3 text-center text-xs">{t('loading')}</p>}<div className="mt-5 grid grid-cols-3 gap-3 border-t border-fl-border pt-4 text-center"><div><p className="text-fl-fg text-lg font-black">{recentHistory.reduce((a, e) => a + e.exercises_total, 0)}</p><p className="text-fl-muted-3 text-[10px] font-mono tracking-widest uppercase">{t('exercises')}</p></div><div><p className="text-fl-fg text-lg font-black">{recentHistory.reduce((a, e) => a + e.lessons_completed, 0)}</p><p className="text-fl-muted-3 text-[10px] font-mono tracking-widest uppercase">{t('lessons')}</p></div><div><p className="text-fl-fg text-lg font-black">{recentHistory.reduce((a, e) => a + e.xp_earned, 0)}</p><p className="text-fl-muted-3 text-[10px] font-mono tracking-widest uppercase">{t('sevenDayXp')}</p></div></div></section>}

    {levelUnits.length > 0 && <section className="space-y-4"><div className="flex items-center gap-3"><h2 className="text-fl-fg text-sm font-bold">{cefrLevel ? t('competenciesSection', { level: cefrLevel }) : t('competencies')}</h2><div className="bg-fl-border h-px flex-1" /></div>{levelUnits.map((unit) => <UnitCompetencyBlock key={unit.id} unit={unit} record={compMap[unit.id]} />)}{competencies.length === 0 && <div className="border-fl-border bg-fl-surface rounded-2xl border px-6 py-8 text-center"><p className="text-fl-muted-3 text-sm leading-relaxed">{t('noCompetencies')}</p><Link href="/plan" className="mt-4 inline-block text-sm font-medium underline transition-all hover:no-underline" style={{ color: 'var(--juba-primary)' }}>{t('goToMyPlan')}</Link></div>}</section>}

    {displayVocabSets.length > 0 && <section className="space-y-4"><div className="flex items-center gap-3"><h2 className="text-fl-fg text-sm font-bold">{showAllLevels ? t('vocabularySection') : cefrLevel ? t('vocabularyHeader', { level: cefrLevel }) : t('vocabularySection')}</h2><div className="bg-fl-border h-px flex-1" /><span className="text-fl-muted-3 shrink-0 text-xs font-medium">{totalMastered}/{totalDisplayWords} {tVocab('words')}</span></div><div className="bg-fl-surface-2 inline-flex rounded-xl p-1"><button type="button" aria-pressed={!showAllLevels} onClick={() => setShowAllLevels(false)} className={`rounded-lg px-3.5 py-1.5 text-xs font-semibold transition-colors ${!showAllLevels ? 'text-fl-fg bg-[var(--juba-surface)] shadow-sm' : 'text-fl-muted-2 hover:text-fl-fg'}`}>{t('currentLevelOnly')}</button><button type="button" aria-pressed={showAllLevels} onClick={() => setShowAllLevels(true)} className={`rounded-lg px-3.5 py-1.5 text-xs font-semibold transition-colors ${showAllLevels ? 'text-fl-fg bg-[var(--juba-surface)] shadow-sm' : 'text-fl-muted-2 hover:text-fl-fg'}`}>{t('allLevels')}</button></div><div className="border-fl-border bg-fl-surface divide-fl-border divide-y rounded-2xl border">{displayVocabSets.map((s) => { const mastered = s.words.filter((w) => masteredWordSet.has(w.word.toLowerCase())).length; const pct = s.words.length > 0 ? Math.round((mastered / s.words.length) * 100) : 0; return <div key={s.id} className="flex items-center gap-4 px-5 py-3.5"><Link href={`/vocabulary/${s.id}`} className="text-fl-muted-1 hover:text-fl-fg min-w-0 flex-1 truncate text-sm font-medium transition-colors">{s.topic}</Link><div className="flex shrink-0 items-center gap-3"><div className="bg-fl-surface-2 h-1.5 w-24 overflow-hidden rounded-full"><div role="progressbar" aria-valuemin={0} aria-valuemax={100} aria-valuenow={pct} className="h-full rounded-full transition-all duration-500" style={{ width: `${pct}%`, background: 'var(--juba-primary)' }} /></div><span className="text-fl-muted-3 w-12 text-end text-xs tabular-nums">{mastered}/{s.words.length}</span></div></div> })}</div></section>}

    {gameStats && <section className="space-y-4"><div className="flex items-center gap-3"><h2 className="text-fl-fg text-sm font-bold">{t('achievements')}</h2><div className="bg-fl-border h-px flex-1" /></div><div className="border-fl-border bg-fl-surface rounded-2xl border p-5"><div className="mb-4 grid grid-cols-2 gap-3 sm:grid-cols-4"><div><p className="text-fl-muted-3 text-xs">{t('gamesPlayed')}</p><p className="text-fl-fg mt-1 text-lg font-bold">{gameStats.games_played}</p></div><div><p className="text-fl-muted-3 text-xs">{t('bestStreak')}</p><p className="text-fl-fg mt-1 text-lg font-bold">{gameStats.best_correct_streak}</p></div><div><p className="text-fl-muted-3 text-xs">{t('dailyChallenges')}</p><p className="text-fl-fg mt-1 text-lg font-bold">{gameStats.daily_challenges_completed}</p></div><div><p className="text-fl-muted-3 text-xs">{t('unlocked')}</p><p className="text-fl-fg mt-1 text-lg font-bold">{gameStats.achievements.length}</p></div></div>{gameStats.achievements.length > 0 ? <div className="flex flex-wrap gap-2">{gameStats.achievements.map((id) => <span key={id} className="border-fl-border bg-fl-surface-2 text-fl-fg inline-flex items-center gap-2 rounded-full border px-3 py-1.5 text-xs font-semibold"><Award className="h-3.5 w-3.5" style={{ color: 'var(--juba-primary)' }} />{id.replaceAll('_', ' ')}</span>)}</div> : <p className="text-fl-muted-3 text-sm">{t('noAchievements')}</p>}</div></section>}

    {summary && Object.keys(summary.skills).length > 0 && <section className="space-y-4"><div className="flex items-center gap-3"><h2 className="text-fl-fg text-sm font-bold">{t('skills')}</h2><div className="bg-fl-border h-px flex-1" /></div><div className="border-fl-border bg-fl-surface divide-fl-border divide-y rounded-2xl border">{Object.entries(summary.skills).map(([skill, value]) => <div key={skill} className="flex items-center gap-4 px-5 py-3.5"><span className="text-fl-muted-1 w-24 shrink-0 truncate text-sm font-medium">{skill}</span><div className="bg-fl-surface-2 h-1.5 flex-1 overflow-hidden rounded-full"><div role="progressbar" aria-valuemin={0} aria-valuemax={100} aria-valuenow={Math.round(value * 100)} className="h-full rounded-full transition-all duration-500" style={{ width: `${Math.round(value * 100)}%`, background: value < 0.5 ? 'var(--juba-warm)' : 'var(--juba-primary)' }} /></div><span className="text-fl-muted-2 w-10 shrink-0 text-end text-xs font-semibold tabular-nums">{Math.round(value * 100)}%</span></div>)}</div></section>}
  </div>
}
