# Local development API endpoint
# Android/iOS physical devices cannot use localhost to reach the PC running FastAPI.
# Replace YOUR-PC-IP with the LAN IPv4 address of the development machine.
EXPO_PUBLIC_API_URL=http://YOUR-PC-IP:8000
